.profile-page {
  min-height: 100vh;
  background: #f7fafc;
  color: #1a202c;
}

.profile-nav {
  position: fixed;
  top: 0;
  right: 0;
  left: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 92rpx;
  padding-right: 28rpx;
  padding-left: 28rpx;
  border-bottom: 1rpx solid #edf2f7;
  background: rgba(255, 255, 255, 0.97);
  box-shadow: 0 4rpx 18rpx rgba(26, 32, 44, 0.04);
  box-sizing: content-box;
}

.profile-back,
.profile-nav__placeholder {
  width: 72rpx;
  height: 72rpx;
}

.profile-back {
  display: flex;
  align-items: center;
  justify-content: center;
  color: #0e9aa7;
  font-size: 64rpx;
  line-height: 1;
}

.profile-nav__title {
  font-size: 34rpx;
  font-weight: 800;
}

.profile-content {
  padding-right: 32rpx;
  padding-bottom: calc(40rpx + env(safe-area-inset-bottom));
  padding-left: 32rpx;
}

.profile-hero {
  margin-top: 24rpx;
  padding: 36rpx;
  border-radius: 32rpx;
  background: linear-gradient(135deg, #0e9aa7, #3ec6d0);
  color: #fff;
  box-shadow: 0 14rpx 30rpx rgba(14, 154, 167, 0.18);
  margin-bottom: 24rpx;
}

.profile-hero__icon {
  font-size: 60rpx;
}

.profile-hero__title {
  margin-top: 12rpx;
  font-size: 36rpx;
  font-weight: 800;
}

.profile-hero__desc,
.profile-card__desc,
.profile-privacy {
  margin-top: 12rpx;
  font-size: 26rpx;
  line-height: 1.7;
}

.profile-hero__desc {
  opacity: 0.88;
}

.profile-card {
  margin-top: 24rpx;
  padding: 32rpx;
  border: 1rpx solid #edf2f7;
  border-radius: 28rpx;
  background: #fff;
  box-shadow: 0 4rpx 24rpx rgba(14, 154, 167, 0.06);
}

.profile-card--phone {
  margin-bottom: 4rpx;
}

.profile-card__title {
  font-size: 32rpx;
  font-weight: 800;
}

.profile-card__desc,
.profile-privacy {
  color: #718096;
}

.profile-input {
  height: 88rpx;
  margin-top: 24rpx;
  padding: 0 24rpx;
  border: 1rpx solid #e2e8f0;
  box-sizing: border-box;
  border-radius: 16rpx;
  background: #f8fafc;
  font-size: 30rpx;
}

.profile-field-row {
  display: flex;
  align-items: center;
  min-height: 88rpx;
  margin-top: 20rpx;
  padding: 0 24rpx;
  border: 1rpx solid #edf2f7;
  box-sizing: border-box;
  border-radius: 16rpx;
  background: #f8fafc;
  font-size: 30rpx;
}

.profile-field-label {
  width: 120rpx;
  color: #4a5568;
}

.profile-field-input,
.profile-field-value {
  flex: 1;
  min-width: 0;
  color: #1a202c;
}

.profile-field-unit,
.profile-field-arrow {
  color: #718096;
}

.profile-field-arrow {
  margin-left: 16rpx;
  font-size: 42rpx;
  line-height: 1;
}

.profile-nickname-actions {
  display: flex;
  gap: 16rpx;
  margin-top: 20rpx;
}

.profile-wechat-name,
.profile-nickname-save,
.profile-save,
.profile-phone-button {
  margin-top: 20rpx;
  border-radius: 16rpx;
  font-size: 30rpx;
  font-weight: 700;
}

.profile-nickname-actions .profile-wechat-name,
.profile-nickname-actions .profile-nickname-save {
  flex: 1;
  margin-top: 0;
}

.profile-wechat-name {
  color: #087f8b;
  background: #e6f7f8;
}

.profile-nickname-save {
  color: #fff;
  background: #0e9aa7;
}

.profile-card__hint {
  margin-top: 14rpx;
  color: #718096;
  font-size: 24rpx;
  line-height: 1.55;
}

.profile-save,
.profile-phone-button {
  color: #fff;
  background: #0e9aa7;
}

.profile-phone-status {
  margin-top: 20rpx;
  padding: 18rpx 20rpx;
  border-radius: 16rpx;
  color: #2f855a;
  background: #f0fff4;
  font-size: 28rpx;
}

.profile-privacy {
  padding: 28rpx 12rpx 0;
  font-size: 24rpx;
}
